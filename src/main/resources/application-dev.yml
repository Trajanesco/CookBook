spring:
  datasource:
    url: jdbc:mysql://192.168.99.101:3306/sfg_dev
    username: sfg_dev_user
    password: matmic
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
    properties:
      javax:
        persistance:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: matmic_database_create.sql